#!/bin/bash
curl https://sh.rustup.rs -sSf | sh -s -- -y

# It seems the bins are installed in ~/.cargo/bin
# source /home/xiaoyang/.cargo/env will make it immediately works


# NOTE:
# cargo will build software from compiling. It need more dependencies
