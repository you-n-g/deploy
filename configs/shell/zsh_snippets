typeset -g -A zshSnippetArr=( [fpre]='fzf  --preview "echo {} | bat -l  Python --style plain # --color=always" --preview-window :wrap' [jp0]='jupyter notebook --no-browser --port 9000 --ip 0.0.0.0' [jph]='jupyter notebook --no-browser --port 9000 --ip `hostname` # You can not access 127.0.0.1 when binding hostname' [pb]='python -m black .  -l 120' [psd]='sudo env "PATH=$PATH"  py-spy dump --pid ' [psr]='py-spy record -o profile.svg -- ' [pst]='sudo env "PATH=$PATH"  py-spy top --pid ' [ptr]='pstree -pas ' [rgcn]='rg '\''[\p{Han}]'\' [rpr]='autossh -R 6489:127.0.0.1:6489 XXX # http proxy deployed locally, XXX configured in .ssh/config' [rs]='rsync  -avzrP -e ssh XXX XXX  # z(compression) P(progress)' [sn]='snakeviz -s -H `hostname` stats_out' [tl]='tmux list-panes -a -F "#{pane_pid} #{session_name}:#{window_index}:#{pane_index}"' [trz]='terminalizer record XXX -d zsh' )
