typeset -g -A zshSnippetArr=( jp0 'jupyter notebook --no-browser --port 9000 --ip 0.0.0.0' jph 'jupyter notebook --no-browser --port 9000 --ip `hostname` # You can not access 127.0.0.1 when binding hostname' pb 'python -m black .  -l 120' psd 'sudo env "PATH=$PATH"  py-spy dump --pid ' psr 'py-spy record -o profile.svg -- ' pst 'sudo env "PATH=$PATH"  py-spy top --pid ' ptr 'pstree -pas ' rgcn 'rg '\''[\p{Han}]'\' sn 'snakeviz -s -H `hostname` stats_out' tl 'tmux list-panes -a -F "#{pane_pid} #{session_name}:#{window_index}:#{pane_index}"' trz 'terminalizer record XXX -d zsh' )
