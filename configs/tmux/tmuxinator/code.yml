# ~/.tmuxinator/code.yml
name: code
root: ~/

windows:
  - code_repo: cd ~/cheatsheets/
  - deployment: cd ~/deploy/
  - commit:
        layout: even-horizontal
        panes:
            - cd ~/cheatsheets/
            - cd ~/deploy/
